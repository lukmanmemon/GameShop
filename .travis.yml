language: node_js
node_js:
- 14
cache: npm

install: skip

script:
- cd client
- npm test
- npm run build

deploy:
  provider: pages
  skip_cleanup: true
  github-token: $GITHUB_TOKEN
  local-dir: client/build
  on:
    branch: main
