language: node_js
node_js:
- 14
cache: npm

install: skip


script:
- npm test
- npm run build

deploy:
  provider: pages
  skip_cleanup: true
  github-token: $GITHUB_TOKEN
  local-dir: build
  on:
    branch: main
